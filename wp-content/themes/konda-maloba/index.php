<?php
/**
 * The most generic template file in a WordPress theme.
 *
 * Fallback used when no other, more specific template matches
 * the current query in the Template Hierarchy.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
